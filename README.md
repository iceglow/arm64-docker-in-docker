# Docker arm64 in Docker
Emulated arm64 ubuntu running a docker host. Can be used for building arm64 images and such.

## Connect to the arm64 Docker
The container exposes docker over tcp:// at port 2375.
